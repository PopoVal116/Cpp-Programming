
# Лабораторная работа №3 — Текстовые и бинарные файлы

## Теория
**Текстовые файлы** — данные хранятся как символы (нужны преобразования).  
**Бинарные файлы** — данные записываются «как есть» (без преобразования).

**Потоки:**
- `ifstream` — чтение
- `ofstream` — запись
- `ios::binary` — бинарный режим

**Запись/чтение структуры:**
```cpp
write((char*)&obj, sizeof(obj));  // запись
read((char*)&obj, sizeof(obj));   // чтение
Реализовано

Чтение из tea.txt (текстовый)
Запись в бинарный файл tea_binary.dat
Чтение бинарного файла в массив структур
Сортировка по выручке (price * quantity)
Вывод отсортированного списка и общей суммы

Ключевой фрагмент
C++ofstream bin("tea_binary.dat", ios::binary);
bin.write((char*)&tea, sizeof(Tea));
